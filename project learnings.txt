UTF 8 vs  latin-1 encoding
list(row)
for i,row in file_name
conn.commit()
dataframe in pandas-A Data frame is a two-dimensional data structure, i.e., data is aligned in a tabular fashion in rows and columns. Pandas DataFrame consists of three principal components, the data, rows, and columns.
Converting assister_id to float as it has null values, they cannot be done in int datatype
Change of data type in games,shots 
Ensure you given enough parameters

add commit as seperate cell after each data insert
Add secret manager from AWS 

Importance of auto commit 





